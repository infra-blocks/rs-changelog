[package]
name = "changelog-ast"
version = "0.0.0"
edition = "2024"

[dependencies]
# A fork of version 0.13.3 with some modifications that have been submitted on the original repo,
# awaiting review.
pulldown-cmark = { git = "https://github.com/el-pendeloco/pulldown-cmark.git", branch = "next" }
